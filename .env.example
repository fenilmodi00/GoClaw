################################################################################
# GoClaw SaaS Environment Configuration
################################################################################
#
# QUICK START FOR LOCAL DEVELOPMENT:
# ===================================
# 1. Copy this file to .env: cp .env.example .env
# 2. Fill in the values below (see instructions for each section)
# 3. For webhooks without a domain, use ngrok (see webhook sections)
# 4. Run: npm run dev
#
# WEBHOOK TESTING WITH NGROK (No Domain Required):
# =================================================
# ngrok creates a public HTTPS tunnel to your localhost:3000
#
# Steps:
# 1. Download ngrok: https://ngrok.com/download
# 2. Sign up for free account: https://dashboard.ngrok.com
# 3. Run: ngrok http 3000
# 4. Copy the HTTPS URL (e.g., https://abc123.ngrok.io)
# 5. Use this URL for webhook endpoints in Stripe and Clerk
# 6. Webhooks will now forward to your local development server
#
################################################################################

# ============================================================================
# DATABASE CONFIGURATION (Turso)
# ============================================================================
# Turso is a distributed SQLite database service
# Setup: https://turso.tech/
#
# Steps:
# 1. Create account at https://turso.tech
# 2. Install Turso CLI: npm install -g @tursodatabase/cli
# 3. Create database: turso db create goclaw
# 4. Get connection string: turso db show goclaw --http-url
# 5. Get auth token: turso db tokens create goclaw
#
DATABASE_URL=""
DATABASE_AUTH_TOKEN="" 
# ============================================================================
# STRIPE CONFIGURATION
# ============================================================================
# Stripe handles payment processing for bot deployments
# Setup: https://stripe.com
#
# Steps:
# 1. Create account at https://stripe.com
# 2. Go to Dashboard: https://dashboard.stripe.com/apikeys
# 3. Copy Secret Key (starts with sk_test_ or sk_live_)
# 4. Copy Publishable Key (starts with pk_test_ or pk_live_)
#
STRIPE_SECRET_KEY=""
STRIPE_PUBLISHABLE_KEY=""

# Stripe Webhook Secret
# Setup: https://dashboard.stripe.com/webhooks
#
# For LOCAL DEVELOPMENT (without domain):
# 1. Install ngrok: https://ngrok.com/download
# 2. Run: ngrok http 3000
# 3. Copy the HTTPS URL (e.g., https://abc123.ngrok.io)
# 4. Create webhook endpoint: https://abc123.ngrok.io/api/webhooks/stripe
# 5. Select event: checkout.session.completed
# 6. Copy the signing secret (starts with whsec_)
#
# For PRODUCTION (with domain):
# 1. Go to https://dashboard.stripe.com/webhooks
# 2. Create new endpoint: https://yourdomain.com/api/webhooks/stripe
# 3. Select event: checkout.session.completed
# 4. Copy the signing secret
#
STRIPE_WEBHOOK_SECRET=""

# ============================================================================
# POLAR.SH CONFIGURATION
# ============================================================================
# Polar handles payment processing as an alternative to Stripe
# Setup: https://polar.sh
#
# Steps:
# 1. Create account at https://polar.sh
# 2. Go to Settings -> Developers to get Access Token
# 3. Create a Product and get its ID
# 4. Configure Webhooks in Polar settings
#
# Server environment: 'sandbox' or 'production'
POLAR_SERVER="sandbox"
#
# Access Token (starts with polar_at_)
POLAR_ACCESS_TOKEN=""
#
# Webhook Secret (starts with whsec_)
POLAR_WEBHOOK_SECRET=""
#
# Product ID to sell (starts with pro_)
POLAR_PRODUCT_ID=""
#
# Free Tier Product ID (for initial credit grant)
POLAR_FREE_TIER_PRODUCT_ID=""

# ============================================================================
# ENCRYPTION CONFIGURATION
# ============================================================================
# Used to encrypt sensitive API keys before storing in database
# Generate a new key: openssl rand -hex 32
#
# For LOCAL DEVELOPMENT:
# Run: openssl rand -hex 32
# Copy the output and paste it below
#
# IMPORTANT: Keep this secret and never commit to version control
# Use a secure key management service in production
#
ENCRYPTION_KEY=""

# ============================================================================
# APPLICATION CONFIGURATION
# ============================================================================
# Public URL where the application is hosted
# Development: http://localhost:3000
# Production: https://yourdomain.com
#
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# ============================================================================
# AKASH CONSOLE API CONFIGURATION
# ============================================================================
# Akash Console API manages user registration, trials, and deployment settings
# This is the backend API for Akash Network integration
#
# Production: https://console.akash.network/api
# Testnet: https://testnet-console.akash.network/api (if available)
#
AKASH_CONSOLE_API_URL=""

# Akash API Key (REQUIRED)
# This is the system-wide API key used to deploy all user bots to Akash Network
# Obtain this from your Akash Console account settings at https://console.akash.network
#
# IMPORTANT: This key is required for deployments to work. Without it, all deployments will fail.
# Keep this key secure and never commit it to version control.
#
AKASH_API_KEY="" 

# ============================================================================
# AKASHML API CONFIGURATION
# ============================================================================
# AkashML provides AI model inference via OpenAI-compatible API
# The OpenClaw bot uses meta-llama/Llama-3.3-70B-Instruct model
#
# Setup: https://akashml.com or https://api.akashml.com
# Get your API key from the AkashML dashboard
#
# IMPORTANT: This key is embedded in the SDL and deployed with each bot
# Keep this key secure and never commit it to version control.
#
AKASHML_KEY=""

# ============================================================================
# CLERK AUTHENTICATION CONFIGURATION
# ============================================================================
# Clerk handles user registration, login, and session management
# Setup: https://clerk.com
#
# Steps:
# 1. Create account at https://clerk.com
# 2. Create new application
# 3. Go to API Keys section
# 4. Copy Publishable Key (starts with pk_test_ or pk_live_)
# 5. Copy Secret Key (starts with sk_test_ or sk_live_)
#
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=

# Clerk Webhook Secret
# Setup: https://dashboard.clerk.com/webhooks
#
# For LOCAL DEVELOPMENT (without domain):
# 1. Install ngrok: https://ngrok.com/download
# 2. Run: ngrok http 3000
# 3. Copy the HTTPS URL (e.g., https://abc123.ngrok.io)
# 4. Create webhook endpoint: https://abc123.ngrok.io/api/webhooks/clerk
# 5. Select event: user.created
# 6. Copy the signing secret (starts with whsec_)
#
# For PRODUCTION (with domain):
# 1. Go to https://dashboard.clerk.com/webhooks
# 2. Create new endpoint: https://yourdomain.com/api/webhooks/clerk
# 3. Select event: user.created
# 4. Copy the signing secret
#
CLERK_WEBHOOK_SECRET=""

################################################################################
# ENVIRONMENT VALIDATION
################################################################################
# The application will fail to start if any required environment variables
# are missing. All variables above are required for production.
#
# For LOCAL DEVELOPMENT:
# - Use ngrok for webhook testing (see webhook sections above)
# - You can use placeholder values for most variables
# - Webhooks won't work until you configure ngrok tunneling
#
# For PRODUCTION:
# - All variables must be configured with real values
# - Use a domain name for webhook endpoints
# - Store secrets in a secure key management service
################################################################################
